import React from "react";
import { WordListContainer } from "@containers";

export const App: React.FC<{}> = () => {
    return (
        <div>
            <WordListContainer />
        </div>
    );
};
